import { PUBLIC_PAGE_META } from "@/lib/seo/publicPages";

export const metadata = PUBLIC_PAGE_META.upcomingevent;

export default function UpcomingEventsLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
