import { createPillarMetadata, createPillarPage } from "@/lib/knowledge-graph/create-pillar-page";

export const metadata = createPillarMetadata("innovation");

export default createPillarPage("innovation");
