import { datadekhMeta } from "@/lib/seo/publicPages";

export const metadata = datadekhMeta("accomodationdata");

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
