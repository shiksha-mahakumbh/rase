import { datadekhMeta } from "@/lib/seo/publicPages";

export const metadata = datadekhMeta("DelegateForm");

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
