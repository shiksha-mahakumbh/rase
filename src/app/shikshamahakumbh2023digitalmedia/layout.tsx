import { mediaArchiveMeta } from "@/lib/seo/mediaArchives";

export const metadata = mediaArchiveMeta("shikshamahakumbh2023digitalmedia");

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
