import { createPillarMetadata, createPillarPage } from "@/lib/knowledge-graph/create-pillar-page";

export const metadata = createPillarMetadata("teacher-development");

export default createPillarPage("teacher-development");
