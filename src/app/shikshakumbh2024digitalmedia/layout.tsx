import { mediaArchiveMeta } from "@/lib/seo/mediaArchives";

export const metadata = mediaArchiveMeta("shikshakumbh2024digitalmedia");

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
