import { createPillarMetadata, createPillarPage } from "@/lib/knowledge-graph/create-pillar-page";

export const metadata = createPillarMetadata("leadership");

export default createPillarPage("leadership");
