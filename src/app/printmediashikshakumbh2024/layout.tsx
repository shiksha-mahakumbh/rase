import { mediaArchiveMeta } from "@/lib/seo/mediaArchives";

export const metadata = mediaArchiveMeta("printmediashikshakumbh2024");

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
